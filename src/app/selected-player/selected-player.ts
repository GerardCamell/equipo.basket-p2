import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
@Component({
  selector: 'app-selected-player',
  imports: [],
  templateUrl: './selected-player.html',
  styleUrl: './selected-player.css',
})
export class SelectedPlayer {

  //Proximamente pondremos aquí para obtener el jugador seleccionado, ahora no se como se hace :'(
}
